% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categories.R
\name{mwi_category}
\alias{mwi_category}
\title{Map index values onto the five activity classes}
\usage{
mwi_category(x, labels = mwi_activity_levels())
}
\arguments{
\item{x}{Numeric vector of index values in \verb{[0, 1]}, for example the
result of \code{\link[=mwi]{mwi()}}.}

\item{labels}{Character vector of five class labels, ordered from least to
most activity. Defaults to \code{\link[=mwi_activity_levels]{mwi_activity_levels()}}; supply your own to
translate them.}
}
\value{
An ordered factor with five levels.
}
\description{
Converts continuous index values into the five ordered classes used to
communicate the index to the public and to mosquito-control stakeholders.
}
\details{
The class boundaries are\tabular{ll}{
   Index value \tab Class \cr
   \verb{= 0} \tab No activity \cr
   \verb{> 0} and \verb{<= 0.33} \tab Low activity \cr
   \verb{> 0.33} and \verb{<= 0.66} \tab Moderate activity \cr
   \verb{> 0.66} and \verb{< 1} \tab High activity \cr
   \verb{= 1} \tab Very high activity \cr
}


Note that "very high activity" corresponds to the single value 1, which the
index attains only when relative humidity is exactly 95\% and the other two
variables are in their optimal ranges (see \code{\link[=mwi]{mwi()}}). Reported humidity is
usually rounded to whole percentage points, so this occurs in practice,
but it will be rare in continuous data such as reanalysis output.
}
\examples{
mwi_category(c(0, 0.2, 0.5, 0.9, 1))

# Order is respected, so classes can be compared
mwi_category(0.9) > mwi_category(0.2)

# Supply your own labels
labs <- c("none", "low", "moderate", "high", "very high")
mwi_category(c(0, 1), labels = labs)

}
