% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{mwi_summarise}
\alias{mwi_summarise}
\title{Summarise sub-daily weather into index values, in the correct order}
\usage{
mwi_summarise(
  temperature,
  humidity,
  wind_speed,
  by = NULL,
  statistic = c("max", "mean", "min"),
  units = c("km/h", "m/s"),
  na.rm = FALSE
)
}
\arguments{
\item{temperature}{Numeric vector of air temperatures in degrees Celsius.}

\item{humidity}{Numeric vector of relative humidities in percent (0-100).}

\item{wind_speed}{Numeric vector of wind speeds, in the units given by
\code{units}.}

\item{by}{Grouping vector the same length as the weather inputs, typically a
\code{Date} or a day-of-year. Values are summarised within each unique element.
Use \code{NULL} to summarise everything into a single value.}

\item{statistic}{Summary to apply within each group: \code{"max"} (the default),
\code{"mean"}, or \code{"min"}.}

\item{units}{Units of \code{wind_speed}: \code{"km/h"} (the default) or \code{"m/s"}.}

\item{na.rm}{Whether to drop missing index values within each group. If
\code{FALSE} (the default), a group containing any \code{NA} returns \code{NA}.}
}
\value{
A data frame with one row per group and columns \code{by}, \code{mwi} and \code{n}
(the number of observations contributing to the group). If \code{by} is \code{NULL},
a one-row data frame with \code{by = NA}.
}
\description{
Computes the Mosquito Weather Index at the resolution of the supplied
observations and only then summarises it within each group. This is the order
the index requires, and getting it the wrong way round is the most common way
to misuse it.
}
\details{
The index is a product of non-linear response functions, so it does not
commute with averaging:
\code{mwi(mean(temperature), mean(humidity), mean(wind))} is not
\code{mean(mwi(temperature, humidity, wind))}. The discrepancy is severe with
daily maxima, because the daily maximum of relative humidity and the daily
maximum of temperature usually fall many hours apart, so an index computed
from them describes conditions that never co-occurred.

\code{mwi_summarise()} therefore takes sub-daily observations, evaluates
\code{\link[=mwi]{mwi()}} on each one, and reduces the results with \code{statistic} within each
level of \code{by}.
Empirically the maximum is the more informative summary: it captures whether
the period contained a window of favourable conditions, rather than how
favourable the period was on average.
}
\examples{
# One synthetic day of hourly weather
hours <- 0:23
temp <- 18 + 8 * sin((hours - 6) / 24 * 2 * pi)
rh <- 75 - 15 * sin((hours - 6) / 24 * 2 * pi)
wind <- rep(5, 24)

mwi_summarise(temp, rh, wind, by = rep("2019-07-15", 24))

# The wrong order gives a different answer
mwi(max(temp), max(rh), max(wind))

}
\seealso{
\code{\link[=mwi]{mwi()}} for the index itself.
}
